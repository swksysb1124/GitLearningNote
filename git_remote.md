## Git Remote

```git
git remote -v						# 顯示所有 remote repo url
git remote add origin <remote repo url>	# 新增 remote repo url
git remote set-url origin <remote repo url>	# 重設 remote repo url
git remote rm XXX 
```