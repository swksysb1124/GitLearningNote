## Git Status

```git
git status	 					# 確認檔案狀態(unstage, stage, modified)
git status -s						# 使用 short format
git status -v						# 同時顯示已經暫存的變更
git status --ignored					# 也顯示 ignored file (定義在.gitignore)
```